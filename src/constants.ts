export const orderStatus = {
  accepted: 'accepted',
  pending: 'pending',
  delivered: 'delivered',
  shipped: 'shipped'
} as const

export const ROLES = {
  USER: 'USER',
  ADMIN: 'ADMIN'
} as const
